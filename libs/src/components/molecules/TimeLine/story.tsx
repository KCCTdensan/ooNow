import TimeLine from ".";
import React from "react";

export default {
    title: "Molecules / TimeLine",
    component: TimeLine,
}

export const timeLines = () => {
    return (
        <TimeLine />
    )
}