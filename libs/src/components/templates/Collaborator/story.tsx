import Collaborator from "."
import React from "react"

export default {
  title: "Templates / Collaborator",
  component: Collaborator,
}

export const Companies = () => {
  return <Collaborator />
}
